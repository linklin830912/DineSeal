import React from 'react';
import LineProcessbar from '@/components/process-bar/line-process-bar';
export default function MemoryLaneProcessbar() { 
    return (          
        <LineProcessbar totalSteps={0} currentStep={0}/>
    );
}